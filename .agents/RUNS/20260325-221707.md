# RUN 20260325-221707

- Goal: Force Android builds to use Java 17 instead of Java 21.
- Changes:
  - Updated `android/build.gradle` to override `sourceCompatibility` and `targetCompatibility` to Java 17 across Android subprojects and Java compile tasks.
  - Kept generated Capacitor files unchanged.
- Validation:
  - `JAVA_HOME=$(/usr/libexec/java_home -v 17) GRADLE_USER_HOME=/tmp/gen-calories-gradle ./gradlew :capacitor-android:compileDebugJavaWithJavac --no-daemon` ✅
- Notes:
  - Initial attempt also set `options.release`, but Android Gradle rejected that; removed it and re-ran successfully.
