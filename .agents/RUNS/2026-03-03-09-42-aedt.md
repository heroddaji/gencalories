# RUN LOG — 2026-03-03 09:42 AEDT

## Scope

Add support for different serving types in the food entry flow.

## Completed this run

- Added `src/features/food-entry/domain/servingUnits.ts` with:
  - defaults/constants
  - predefined serving unit catalog
  - serving unit synonym normalization
  - predefined unit detection helpers
- Updated `FoodEntryPage` UI to use serving type selector + custom unit input.
- Added suggestion-selection logic to map units into predefined or custom paths.
- Normalized serving units in `createFoodEntry` to keep storage/history canonical.
- Added factory unit test for serving synonym normalization.
- Fixed existing normalization edge case in `normalizeFoodName` (trailing space after punctuation removal).
- Validation passed: `npm run typecheck`, `npm test`.

## Blockers

- None.

## Next run first steps

1. Add integration tests for serving selector and suggestion prefill.
2. Evaluate unit-aware nutrition conversion policy.
3. Run build validation (`npm run build`).
