# RUN 20260325-220536

- Goal: Fix Firebase setup placement for the active Framework7 app path and resolve missing SDK imports.
- Changes:
  - Added `src/shared/firebase/app.ts` with shared Firebase app initialization and guarded analytics startup.
  - Removed Firebase initialization from `src/main.tsx`.
  - Wired Firebase analytics initialization into `src/app/App2.tsx`.
  - Installed `firebase` and synced native Capacitor projects with `npm run cap:sync`.
- Validation:
  - `npm run typecheck` ✅
  - `npm run build` ✅
  - `npm run cap:sync` ✅
- Notes:
  - Capacitor sync reported the existing warning that `@capacitor-community/sqlite` does not ship `Package.swift`, so iOS SPM compatibility remains partial for that package.
