# Run Journal

- Timestamp: **2026-03-25**
- Task: Remove custom styling, use Framework7 bundle CSS only, and force the demo app into iOS theme.

## Changes made

- Set `theme: "ios"` in `src/app/App2.tsx`.
- Removed custom class hooks from the Framework7 demo markup.
- Removed the `@/theme.css` import from `src/main.tsx`.
- Deleted `src/theme.css`.

## Validation

- `npm run build` ✅
- Result: build succeeds with Framework7 bundle styling only; Vite still reports the existing large chunk warning.
